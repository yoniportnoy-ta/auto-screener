// Placeholder. The Auto Screener backend overwrites the body of this file
// at /extension.zip generation time, injecting the live SCREENER_API_TOKEN
// from the server's environment so recruiters never have to paste it.
//
// If you're seeing this exact content in your installed extension, you
// installed from Git (not from /extension.zip on the running server),
// so you'll need to paste the token manually in the popup.

window.AS_EMBEDDED_TOKEN = "";
window.AS_EMBEDDED_BACKEND = "";
